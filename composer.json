{
    "name": "seoladan/bailiocht",
    "description": "Bailíocht Validation library",
    "version": "1.0.0",
    "type": "library",
    "minimum-stability": "stable",
    "license": "MIT",
    "authors": [
        {
            "name": "Sean Ellingham",
            "email": "sean@terrestris.co.uk"
        }
    ],
    "require": {
        "php": ">=8.2",
        "psr/container": "^2.0",
        "seoladan/datetime": "^1.1.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5.0",
        "marcocesarato/php-conventional-changelog": "^1.17",
        "symfony/console": "^7",
        "brainmaestro/composer-git-hooks": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Seoladan\\Bailiocht\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Seoladan\\Bailiocht\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "changelog": "vendor/bin/conventional-changelog",
        "release": "vendor/bin/conventional-changelog --commit",
        "release:patch": "vendor/bin/conventional-changelog --commit --patch",
        "release:minor": "vendor/bin/conventional-changelog --commit --minor",
        "release:major": "vendor/bin/conventional-changelog --commit --major",
        "hooks": "vendor/bin/cghooks",
        "test": "vendor/bin/phpunit"
    },
    "extra": {
        "hooks": {
            "pre-commit": [
                "composer validate --no-check-version"
            ],
            "pre-push": [
                "composer test"
            ]
        }
    },
    "repositories": [
        {
            "type": "composer",
            "url": "https://satis.seoladan.dev"
        }
    ]
}